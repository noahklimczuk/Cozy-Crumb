# Screens

Captured from `claude/measure-the-gap` at `c244187378f09fdae32911eede0f2c7ddf1875c2`.

Five tabs each way, plus `-recipe` and `-mealPlan` —
two screens that are not tab roots and had never once been
photographed.

On the wide phone: `light`, `dark`, `ax5`. On the narrow
one: `narrow`, `narrow-ax5` — the second install goes on a
standard iPhone, and those ~47pt are where truncation lives.

Shot on:

    wide=iPhone 17 Pro Max
    narrow=iPhone 17

`narrow=none` means the runner had no plain iPhone and
that pass was skipped — the captures are wide-only.

Rebuilt and force-pushed by every run of the iOS workflow —
this branch is a noticeboard, not history. Nothing merges it.

Run: https://github.com/noahklimczuk/Cozy-Crumb/actions/runs/33348825351
