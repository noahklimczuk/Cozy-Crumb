# Git Workflow Rules

- **Branching**: For every new prompt/task, branch off the latest `main` branch with a descriptive branch name (e.g., `feat/...`, `fix/...`, `chore/...`).
- **Pull Requests**: Every new task/prompt should result in a fresh Pull Request created for that branch.
