<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    Template for the key a build ships with. See GeminiAPIKey.swift.

    Copy it into place and fill in the key:

        cp Config/Secrets.example.plist "Cozy Crumb/Resources/Secrets.plist"

    The copy is git-ignored on purpose. This one, with its empty value, is the
    only version that belongs in the repository.
-->
<plist version="1.0">
<dict>
    <key>GeminiAPIKey</key>
    <string></string>
</dict>
</plist>
